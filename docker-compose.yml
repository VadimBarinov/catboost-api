services:
  app:
    build:
      context: .
      args:
        BASE_IMAGE: python:3.13-slim
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app
    stdin_open: true
    tty: true
